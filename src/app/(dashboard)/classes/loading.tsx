import { PageSkeleton } from '@/components/ui/page-skeleton'

/**
 * Classes Page Loading State
 *
 * 수업 관리 페이지의 로딩 상태입니다.
 */
export default function ClassesLoading() {
  return <PageSkeleton variant="list" />
}
